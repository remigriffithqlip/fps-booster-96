const defaultConfig = { fps: 60, resolution: '1920x1080', quality: 'high' };function validateConfig(config) { if (typeof config.fps !== 'number' || config.fps <= 0) { throw new Error('Invalid fps value'); } if (!/\d+x\d+/.test(config.resolution)) { throw new Error('Invalid resolution format'); } if (!['low', 'medium', 'high'].includes(config.quality)) { throw new Error('Invalid quality setting'); } return true; }function loadConfig(userConfig) { try { validateConfig(userConfig); return { ...defaultConfig, ...userConfig }; } catch (error) { console.error(`Config load error: ${error.message}`); return defaultConfig; }}export { loadConfig };